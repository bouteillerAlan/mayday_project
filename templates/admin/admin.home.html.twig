{% extends 'base.html.twig' %}

{% block title %}Administration du blog{% endblock %}



{% block nav %}
    <!--navbar-->
    <nav id="spy">
        <div class="nav-wrapper" data-target="top">
            <a href="#" class="brand-logo center">mayday<span class="blink">_</span></a>
            <a href="#" data-target="slide-out" class="sidenav-trigger"><i class="material-icons">menu</i></a>
            <ul class="left hide-on-med-and-down">
                <li><a href="{{ path('home') }}">Accueil du site</a></li>
                <li><a href="{{ path('admin') }}">Administration</a></li>
            </ul>
        </div>
    </nav>

    <!--Mobile structure-->
    <ul class="sidenav" id="slide-out">
        <li><a href="{{ path('home') }}">Accueil du site</a></li>
        <li><a href="{{ path('admin') }}">Administration</a></li>
    </ul>
    <!--navbar end-->
{% endblock %}

{% block body %}
    <div class="row container">
        <div class="col s12">
            <table class="highlight">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Date de création</th>
                        <th>Status</th>
                        <th>Modification</th>
                    </tr>
                </thead>

                <tbody>
                    {% for data in result %}
                    <tr>
                        <td>{{ data.title }}</td>
                        <td>{{ data.date }}</td>
                        <td>{{ data.status }}</td>
                        <td>
                            <a class="waves-effect waves-light btn-small"><i class="material-icons left">burst_mode</i>Modifier</a>
                            <a class="waves-effect waves-light btn-small"><i class="material-icons left">backspace</i>Supprimer</a>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
