{% extends 'admin/admin.home.html.twig' %}

{% block title %}Admin - {{ result.slug }}{% endblock %}

{% block body %}
    <div class="row container">
        <div class="col s12 center-align">
            <h1>Vous modifiez l'entrée de blog n°{{ result.id }}</h1>
        </div>

        <div class="col s12">
            {{ form_start(form) }}
                {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>

    </div>
{% endblock %}
